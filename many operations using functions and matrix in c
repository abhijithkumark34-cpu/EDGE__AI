// Online C compiler to run C program online
#include <stdio.h>
#include<string.h>
void multiply(int m,int n,int p,int q,int a[m][n],int b[p][q],int c[m][q]);
void sum(int m,int n,int a[m][n]);
void transpose(int m,int n,int a[m][n]);

int main() {
    int m,n,p,q;
    printf("Enter the number of roes and coloum for mtrix A");
    scanf("%d%d",&m,&n);
     printf("Enter the number of roes and coloum for mtrix B");
     scanf("%d%d",&p,&q);
     int a[m][n];
     int b[p][q];
     int c[m][q];
     
   printf("Enter all elements in matrix 1");
    for(int i=0;i<m;i++){
        for(int j=0;j<n;j++){
            scanf("%d",&a[i][j]);
        }
    }
    printf("Enter all elements in the matrix 2");
     for(int i=0;i<p;i++){
        for(int j=0;j<q;j++){
            scanf("%d",&b[i][j]);
        }
     }
for(int i=0;i<3;i++){
      char hi[5];
     printf("enter the letter for any operations Mu ,SA, TB");
     scanf("%s",hi);
     
     if(strcmp(hi,"M")==0){
     
     multiply(m,n,p,q,a,b,c);
     return 0;
     }
     else if(strcmp(hi,"SA")==0){
          sum( m, n, a);
          
         return 0;
     }
     else if(strcmp(hi,"TB")==0){
        transpose( m, n, a); 
            return 0;
     }
     
     
     else{
         printf("Enter the valide operation");
            return 0;
     }
     }
     

}
void multiply(int m,int n,int p,int q,int a[m][n],int b[p][q],int c[m][q]){
       for(int i=0;i<m;i++){
        for(int j=0;j<q;j++){
            c[i][j]=0;
             for(int k=0;k<n;k++){
                 
                 c[i][j]=c[i][j]+a[i][k]*b[k][j];
                 }
             }
        }
       
           for(int i=0;i<m;i++){
        for(int j=0;j<q;j++){
            printf("%d\t",c[i][j]);
        }
        printf("\n");
           }
       
           }
           void sum(int m,int n,int a[m][n]){
               int sum=0;
               for(int i=0;i<m;i++){
        for(int j=0;j<n;j++){
            sum+=a[i][j];
            
               
           }
           printf("the sum is ",sum);
               }
           }
           void transpose(int m,int n,int a[m][n]){
                   for(int i=0;i<m;i++){
        for(int j=0;j<n;j++){
            a[i][j]=a[j][i];
           }
                   }
                      for(int i=0;i<m;i++){
        for(int j=0;j<n;j++){
            printf("%d\t",a[i][j]);
        }
        printf("\n");
           }
           }
